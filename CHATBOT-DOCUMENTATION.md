# مستندات سیستم چت هوشمند 🤖

## 📋 نمای کلی

سیستم چت مایسا یک **ربات پشتیبانی هوشمند** است که با استفاده از **Pattern Matching پیشرفته** و **Context Awareness** به سوالات کاربران پاسخ می‌دهد.

---

## 🧠 نحوه کار

### 1. معماری سیستم

```
کاربر → ورودی پیام → تحلیل متن → Pattern Matching → انتخاب بهترین پاسخ → نمایش پاسخ
```

### 2. الگوریتم پاسخ‌دهی

#### مرحله 1: پردازش ورودی
```typescript
const lowerMessage = userMessage.toLowerCase().trim();
```
- تبدیل به حروف کوچک
- حذف فاصله‌های اضافی

#### مرحله 2: Pattern Matching
```typescript
for (const knowledge of botKnowledge) {
  for (const pattern of knowledge.patterns) {
    if (lowerMessage.includes(pattern.toLowerCase())) {
      // محاسبه امتیاز
      const score = pattern.length;
      // ذخیره بهترین match
    }
  }
}
```

#### مرحله 3: Scoring System
- هر pattern که match شود، امتیاز می‌گیرد
- pattern‌های طولانی‌تر امتیاز بیشتری دارند
- بهترین match انتخاب می‌شود

#### مرحله 4: Response Selection
- اگر چند پاسخ وجود داشته باشد، یکی به صورت تصادفی انتخاب می‌شود
- این باعث می‌شود مکالمه طبیعی‌تر باشد

---

## 📚 پایگاه دانش (Knowledge Base)

### ساختار هر دانش:

```typescript
{
  patterns: string[],      // کلمات کلیدی
  responses: string[],     // پاسخ‌های ممکن
  category: string         // دسته‌بندی
}
```

### دسته‌بندی‌های موجود:

1. **greeting** - سلام و احوال‌پرسی
2. **order_tracking** - پیگیری سفارش
3. **how_to_order** - نحوه خرید
4. **shipping_cost** - هزینه ارسال
5. **delivery_time** - زمان تحویل
6. **support** - پشتیبانی
7. **return** - بازگشت کالا
8. **discount** - تخفیف‌ها
9. **price** - قیمت
10. **quality** - کیفیت
11. **payment** - پرداخت
12. **thanks** - تشکر
13. **goodbye** - خداحافظی

---

## 🎯 ویژگی‌های هوشمند

### 1. Multi-Pattern Matching
هر موضوع چندین pattern دارد:
```typescript
patterns: ['سلام', 'درود', 'صبح بخیر', 'hi', 'hello']
```

### 2. Multi-Language Support
پشتیبانی از فارسی و انگلیسی:
```typescript
patterns: ['تخفیف', 'کد تخفیف', 'discount', 'کوپن', 'coupon']
```

### 3. Context Awareness
ربات می‌تواند از context استفاده کند:
```typescript
// مثال: اگر کاربر "سفارش" بگوید
// ربات می‌فهمد منظورش "نحوه سفارش" یا "پیگیری سفارش" است
```

### 4. Random Response Selection
برای طبیعی‌تر بودن مکالمه:
```typescript
const randomResponse = responses[Math.floor(Math.random() * responses.length)];
```

### 5. Helpful Default Response
اگر ربات نفهمید، پیشنهادات می‌دهد:
```typescript
return `متوجه سوال شما نشدم. 🤔

می‌تونید از این موضوعات بپرسید:
• وضعیت سفارش
• نحوه خرید
• هزینه ارسال
...`;
```

---

## 💡 مثال‌های کاربردی

### مثال 1: سلام ساده
```
کاربر: سلام
ربات: سلام! به فروشگاه مایسا خوش آمدید. چطور می‌تونم کمکتون کنم؟
```

### مثال 2: سوال درباره ارسال
```
کاربر: هزینه ارسال چقدره؟
ربات: 📦 هزینه ارسال:

✅ سفارشات بالای 500 هزار تومان: رایگان
💰 سفارشات زیر 500 هزار تومان: 30 هزار تومان

🚚 ارسال با پست پیشتاز و تیپاکس
```

### مثال 3: سوال پیچیده
```
کاربر: چطور میتونم سفارش بدم و چقدر طول میکشه تا برسه؟
ربات: [پاسخ درباره نحوه سفارش - چون "سفارش" امتیاز بیشتری داره]
```

---

## 🔧 نحوه افزودن دانش جدید

### مرحله 1: تعریف Pattern
```typescript
{
  patterns: ['کلمه کلیدی 1', 'کلمه کلیدی 2', ...],
  responses: ['پاسخ 1', 'پاسخ 2', ...],
  category: 'نام دسته'
}
```

### مرحله 2: افزودن به botKnowledge
```typescript
const botKnowledge = [
  // ... دانش‌های قبلی
  {
    patterns: ['موضوع جدید', 'کلمه کلیدی'],
    responses: ['پاسخ مناسب'],
    category: 'new_category'
  }
];
```

### مثال: افزودن دانش درباره گارانتی
```typescript
{
  patterns: ['گارانتی', 'warranty', 'ضمانت نامه'],
  responses: [
    '🛡️ گارانتی محصولات:\n\n✅ گارانتی معتبر\n✅ پشتیبانی کامل\n✅ تعویض در صورت مشکل\n\nمدت گارانتی بسته به محصول متفاوت است.'
  ],
  category: 'warranty'
}
```

---

## 📊 آمار و تحلیل

### Metrics قابل اندازه‌گیری:

1. **Response Rate**: درصد سوالاتی که ربات پاسخ داد
2. **Match Score**: میانگین امتیاز match‌ها
3. **Category Distribution**: توزیع سوالات در دسته‌بندی‌ها
4. **User Satisfaction**: رضایت کاربر از پاسخ‌ها

### نحوه پیاده‌سازی Analytics:

```typescript
// در آینده می‌توان اضافه کرد:
const logInteraction = (userMessage: string, botResponse: string, category: string) => {
  // ذخیره در database
  // تحلیل pattern‌ها
  // بهبود پاسخ‌ها
};
```

---

## 🚀 بهبودهای آینده

### 1. Machine Learning Integration
```typescript
// استفاده از NLP برای درک بهتر
import { NaturalLanguageProcessor } from 'some-nlp-library';

const nlp = new NaturalLanguageProcessor();
const intent = nlp.detectIntent(userMessage);
```

### 2. Context Memory
```typescript
// ذخیره context مکالمه
const conversationContext = {
  lastTopic: 'shipping',
  userPreferences: {...},
  previousQuestions: [...]
};
```

### 3. Sentiment Analysis
```typescript
// تشخیص احساسات کاربر
const sentiment = analyzeSentiment(userMessage);
if (sentiment === 'negative') {
  // پاسخ با لحن دلجویانه
}
```

### 4. Multi-turn Conversations
```typescript
// مکالمات چند مرحله‌ای
if (context.waitingForOrderId) {
  // پردازش کد پیگیری
  return trackOrder(userMessage);
}
```

### 5. Integration با Backend
```typescript
// اتصال به API واقعی
const orderStatus = await fetchOrderStatus(orderId);
return `وضعیت سفارش شما: ${orderStatus}`;
```

### 6. Voice Support
```typescript
// پشتیبانی از ورودی صوتی
const text = await speechToText(audioInput);
const response = getBotResponse(text);
await textToSpeech(response);
```

---

## 🔒 امنیت و Privacy

### 1. Data Protection
- هیچ اطلاعات حساسی ذخیره نمی‌شود
- پیام‌ها فقط در client-side هستند
- بدون ارسال به سرور خارجی

### 2. Input Validation
```typescript
// اعتبارسنجی ورودی
if (userMessage.length > 500) {
  return 'پیام شما خیلی طولانی است. لطفاً کوتاه‌تر بنویسید.';
}
```

### 3. XSS Prevention
```typescript
// جلوگیری از XSS
const sanitizedMessage = sanitizeInput(userMessage);
```

---

## 📝 Best Practices

### 1. نوشتن Pattern‌های خوب
✅ **خوب:**
```typescript
patterns: ['هزینه ارسال', 'پست', 'حمل', 'shipping']
```

❌ **بد:**
```typescript
patterns: ['ه', 'ا', 'س'] // خیلی کوتاه و مبهم
```

### 2. نوشتن پاسخ‌های خوب
✅ **خوب:**
```typescript
responses: [
  '📦 هزینه ارسال:\n\n✅ بالای 500 هزار: رایگان\n💰 زیر 500 هزار: 30 هزار تومان'
]
```

❌ **بد:**
```typescript
responses: ['30 تومان'] // خیلی کوتاه و ناقص
```

### 3. استفاده از Emoji
- برای جذاب‌تر کردن پاسخ‌ها
- برای دسته‌بندی بصری
- نباید زیاد باشد

### 4. ساختار پاسخ‌ها
```typescript
// ساختار خوب:
`عنوان:\n\nنکته 1\nنکته 2\nنکته 3\n\nاطلاعات تماس`
```

---

## 🧪 تست

### Test Cases:

```typescript
// Test 1: سلام ساده
expect(getBotResponse('سلام')).toContain('خوش آمدید');

// Test 2: سوال درباره ارسال
expect(getBotResponse('هزینه ارسال')).toContain('500 هزار');

// Test 3: سوال نامفهوم
expect(getBotResponse('asdfghjkl')).toContain('متوجه نشدم');

// Test 4: پیام خالی
expect(getBotResponse('')).toContain('لطفاً سوال');
```

---

## 📚 منابع

- [Natural Language Processing](https://en.wikipedia.org/wiki/Natural_language_processing)
- [Chatbot Design Best Practices](https://www.nngroup.com/articles/chatbot-design/)
- [Pattern Matching Algorithms](https://en.wikipedia.org/wiki/Pattern_matching)

---

## 🤝 مشارکت

برای بهبود ربات:
1. Pattern‌های جدید اضافه کنید
2. پاسخ‌های بهتر بنویسید
3. Bug‌ها را گزارش دهید
4. ایده‌های جدید پیشنهاد دهید

---

**ساخته شده با ❤️ برای فروشگاه مایسا**
