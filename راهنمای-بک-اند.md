# 🚀 راهنمای کامل بک‌اند NestJS - فروشگاه مایسا

## ✅ چیزهایی که ساخته شد

یک بک‌اند کامل و حرفه‌ای با:

### 🎯 تکنولوژی‌ها
- **NestJS** - فریمورک Node.js مدرن و قدرتمند
- **Prisma** - ORM مدرن با Type Safety کامل
- **PostgreSQL** - دیتابیس قدرتمند
- **JWT** - احراز هویت امن
- **Swagger** - مستندات خودکار API
- **TypeScript** - Type Safety کامل
- **bcrypt** - رمزنگاری رمز عبور

### 📦 ماژول‌های پیاده‌سازی شده

#### 1. Auth Module (احراز هویت)
- ✅ ثبت‌نام کاربر جدید
- ✅ ورود به سیستم
- ✅ دریافت اطلاعات کاربر فعلی
- ✅ JWT Token
- ✅ Password Hashing

#### 2. Users Module (کاربران)
- ✅ لیست همه کاربران (فقط سوپر ادمین)
- ✅ دریافت اطلاعات یک کاربر
- ✅ ویرایش پروفایل
- ✅ تغییر نقش کاربر (فقط سوپر ادمین)
- ✅ فعال/غیرفعال کردن کاربر

#### 3. Products Module (محصولات)
- ✅ لیست محصولات با فیلتر و جستجو
- ✅ Pagination (صفحه‌بندی)
- ✅ Sort (مرتب‌سازی)
- ✅ دریافت محصول با ID یا Slug
- ✅ ایجاد محصول (فقط ادمین)
- ✅ ویرایش محصول (فقط ادمین)
- ✅ حذف محصول (فقط ادمین)
- ✅ تصاویر چندگانه
- ✅ Variants (رنگ، سایز، موجودی)

#### 4. Categories Module (دسته‌بندی‌ها)
- ✅ لیست دسته‌بندی‌ها
- ✅ دسته‌بندی‌های تو در تو
- ✅ CRUD کامل (فقط ادمین)

#### 5. Orders Module (سفارشات)
- ✅ ایجاد سفارش
- ✅ لیست سفارشات
- ✅ جزئیات سفارش
- ✅ تغییر وضعیت (فقط ادمین)

#### 6. Reviews Module (نظرات)
- ✅ ثبت نظر
- ✅ لیست نظرات محصول
- ✅ تایید نظر (فقط ادمین)
- ✅ حذف نظر (فقط ادمین)

### 🔐 سیستم نقش‌ها (RBAC)

#### Customer (مشتری)
- خرید محصولات
- مدیریت سبد خرید
- ثبت نظر
- مشاهده سفارشات خود

#### Admin (ادمین)
- همه دسترسی‌های Customer +
- مدیریت محصولات
- مدیریت دسته‌بندی‌ها
- مشاهده همه سفارشات
- تایید/رد نظرات

#### Super Admin (سوپر ادمین)
- همه دسترسی‌های Admin +
- مدیریت کاربران
- تغییر نقش کاربران
- دسترسی کامل

---

## 🛠️ نصب و راه‌اندازی

### مرحله 1: نصب Dependencies

```bash
npm install
```

### مرحله 2: تنظیم Database

1. PostgreSQL رو نصب کن
2. یک دیتابیس جدید بساز:

```sql
CREATE DATABASE maysa_shop;
```

3. فایل `.env` رو بساز:

```bash
cp .env.example .env
```

4. `.env` رو ویرایش کن:

```env
DATABASE_URL="postgresql://username:password@localhost:5432/maysa_shop"
JWT_SECRET="your-secret-key-here"
PORT=4000
FRONTEND_URL="http://localhost:3000"
```

### مرحله 3: اجرای Prisma

```bash
# Generate Prisma Client
npm run prisma:generate

# اجرای Migrations (ایجاد جداول)
npm run prisma:migrate

# Seed داده‌های اولیه
npm run prisma:seed
```

### مرحله 4: اجرای سرور

```bash
# Development mode (با watch)
npm run nest:dev

# Production mode
npm run nest:build
npm run nest:start:prod
```

سرور در آدرس `http://localhost:4000` اجرا می‌شود.

---

## 🔑 اطلاعات ورود پیش‌فرض

بعد از اجرای seed، این کاربران ایجاد می‌شوند:

### سوپر ادمین
```
ایمیل: admin@maysa.com
رمز عبور: Admin@123
```

### ادمین
```
ایمیل: support@maysa.com
رمز عبور: Admin@123
```

### مشتری نمونه
```
ایمیل: user@example.com
رمز عبور: User@123
```

---

## 📚 مستندات API

بعد از اجرای سرور، Swagger در این آدرس در دسترس است:

```
http://localhost:4000/api/docs
```

در Swagger می‌تونی:
- همه endpoints رو ببینی
- مستقیماً API رو تست کنی
- مثال‌های request و response رو ببینی
- Bearer Token رو تنظیم کنی

---

## 🧪 تست سریع

### 1. ورود به سیستم

```bash
curl -X POST http://localhost:4000/auth/login \
  -H "Content-Type: application/json" \
  -d '{
    "email": "admin@maysa.com",
    "password": "Admin@123"
  }'
```

**پاسخ:**
```json
{
  "user": {
    "id": "uuid",
    "email": "admin@maysa.com",
    "firstName": "مدیر",
    "lastName": "سیستم",
    "role": "super_admin"
  },
  "token": "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9..."
}
```

### 2. دریافت محصولات

```bash
curl http://localhost:4000/products
```

### 3. دریافت اطلاعات کاربر فعلی

```bash
curl http://localhost:4000/auth/me \
  -H "Authorization: Bearer YOUR_TOKEN_HERE"
```

### 4. ایجاد محصول جدید (فقط ادمین)

```bash
curl -X POST http://localhost:4000/products \
  -H "Content-Type: application/json" \
  -H "Authorization: Bearer YOUR_TOKEN" \
  -d '{
    "name": "کیف دستی چرمی",
    "slug": "leather-handbag",
    "description": "کیف دستی زنانه چرم طبیعی",
    "categoryId": "category-uuid",
    "basePrice": 2500000,
    "discountPercentage": 15,
    "tags": ["چرمی", "زنانه", "دستی"]
  }'
```

---

## 📊 Endpoints اصلی

### 🔐 Authentication

| Method | Endpoint | توضیحات | دسترسی |
|--------|----------|---------|--------|
| POST | `/auth/register` | ثبت‌نام کاربر جدید | همه |
| POST | `/auth/login` | ورود به سیستم | همه |
| GET | `/auth/me` | اطلاعات کاربر فعلی | کاربر لاگین شده |

### 👥 Users

| Method | Endpoint | توضیحات | دسترسی |
|--------|----------|---------|--------|
| GET | `/users` | لیست همه کاربران | Super Admin |
| GET | `/users/:id` | دریافت یک کاربر | همه |
| PATCH | `/users/:id` | ویرایش کاربر | خودش یا ادمین |
| PATCH | `/users/:id/role` | تغییر نقش | Super Admin |
| PATCH | `/users/:id/toggle-active` | فعال/غیرفعال | Super Admin |

### 📦 Products

| Method | Endpoint | توضیحات | دسترسی |
|--------|----------|---------|--------|
| GET | `/products` | لیست محصولات | همه |
| GET | `/products/:id` | دریافت محصول | همه |
| GET | `/products/slug/:slug` | دریافت با slug | همه |
| POST | `/products` | ایجاد محصول | Admin |
| PATCH | `/products/:id` | ویرایش محصول | Admin |
| DELETE | `/products/:id` | حذف محصول | Admin |

**فیلترهای محصولات:**
```
GET /products?page=1&limit=20&categoryId=xxx&minPrice=100000&maxPrice=500000&search=کیف&sortBy=finalPrice&sortOrder=asc&isFeatured=true
```

### 📂 Categories

| Method | Endpoint | توضیحات | دسترسی |
|--------|----------|---------|--------|
| GET | `/categories` | لیست دسته‌بندی‌ها | همه |
| GET | `/categories/:id` | دریافت دسته‌بندی | همه |
| POST | `/categories` | ایجاد دسته‌بندی | Admin |
| PATCH | `/categories/:id` | ویرایش | Admin |
| DELETE | `/categories/:id` | حذف | Admin |

### 🛒 Orders

| Method | Endpoint | توضیحات | دسترسی |
|--------|----------|---------|--------|
| GET | `/orders` | لیست سفارشات | کاربر (خودش) / ادمین (همه) |
| GET | `/orders/:id` | جزئیات سفارش | صاحب سفارش / ادمین |
| POST | `/orders` | ایجاد سفارش | کاربر لاگین شده |
| PATCH | `/orders/:id/status` | تغییر وضعیت | Admin |

### ⭐ Reviews

| Method | Endpoint | توضیحات | دسترسی |
|--------|----------|---------|--------|
| GET | `/reviews/product/:id` | نظرات محصول | همه |
| POST | `/reviews` | ثبت نظر | کاربر لاگین شده |
| PATCH | `/reviews/:id/approve` | تایید نظر | Admin |
| DELETE | `/reviews/:id` | حذف نظر | Admin |

---

## 🔧 دستورات مفید

### NestJS

```bash
# Development (با watch mode)
npm run nest:dev

# Build
npm run nest:build

# Production
npm run nest:start:prod
```

### Prisma

```bash
# Generate Client
npm run prisma:generate

# اجرای Migrations
npm run prisma:migrate

# Prisma Studio (مدیریت دیتابیس)
npm run prisma:studio

# Seed داده‌های اولیه
npm run prisma:seed

# Reset Database
npx prisma migrate reset

# ایجاد Migration جدید
npx prisma migrate dev --name migration_name
```

---

## 📁 ساختار پروژه

```
src/
├── auth/                       # احراز هویت
│   ├── decorators/            # @GetUser, @Roles
│   ├── dto/                   # RegisterDto, LoginDto
│   ├── guards/                # JwtAuthGuard, RolesGuard
│   ├── strategies/            # JWT Strategy
│   ├── auth.controller.ts
│   ├── auth.service.ts
│   └── auth.module.ts
│
├── users/                      # مدیریت کاربران
│   ├── dto/
│   ├── users.controller.ts
│   ├── users.service.ts
│   └── users.module.ts
│
├── products/                   # محصولات
│   ├── dto/
│   ├── products.controller.ts
│   ├── products.service.ts
│   └── products.module.ts
│
├── categories/                 # دسته‌بندی‌ها
├── orders/                     # سفارشات
├── reviews/                    # نظرات
│
├── prisma/                     # Prisma Service
│   ├── prisma.service.ts
│   └── prisma.module.ts
│
├── app.module.ts               # Root Module
└── main.ts                     # Entry Point

prisma/
├── schema.prisma               # Database Schema
├── prisma.config.ts            # Prisma Config
├── seed.ts                     # Seed Data
└── migrations/                 # Database Migrations
```

---

## 🎨 نحوه استفاده از Guards و Decorators

### محافظت از Endpoint با JWT

```typescript
@UseGuards(JwtAuthGuard)
@Get('profile')
getProfile(@GetUser() user: User) {
  return user;
}
```

### محافظت با نقش خاص

```typescript
@UseGuards(JwtAuthGuard, RolesGuard)
@Roles(UserRole.admin, UserRole.super_admin)
@Post('products')
createProduct(@Body() dto: CreateProductDto) {
  // فقط ادمین و سوپر ادمین
}
```

### دریافت اطلاعات کاربر

```typescript
@Get('me')
@UseGuards(JwtAuthGuard)
getMe(
  @GetUser() user: User,           // کل اطلاعات کاربر
  @GetUser('id') userId: string,   // فقط ID
  @GetUser('role') role: UserRole, // فقط نقش
) {
  return user;
}
```

---

## 🔒 امنیت

### چیزهایی که پیاده‌سازی شده:

✅ **Password Hashing** - bcrypt با 10 rounds
✅ **JWT Token** - با expiration time
✅ **Role-based Access** - 3 سطح دسترسی
✅ **Input Validation** - class-validator
✅ **SQL Injection Prevention** - Prisma ORM
✅ **CORS** - تنظیم شده برای frontend
✅ **Environment Variables** - اطلاعات حساس در .env

### نکات امنیتی:

⚠️ **در Production:**
- JWT_SECRET رو تغییر بده
- DATABASE_URL رو امن نگه دار
- HTTPS استفاده کن
- Rate Limiting اضافه کن
- Helmet.js نصب کن

---

## 🐛 عیب‌یابی

### خطای اتصال به Database

```bash
# بررسی PostgreSQL
psql -U postgres

# بررسی DATABASE_URL در .env
echo $DATABASE_URL
```

### خطای Prisma Client

```bash
# دوباره Generate کن
npm run prisma:generate
```

### خطای Migration

```bash
# Reset و دوباره Migrate
npx prisma migrate reset
npm run prisma:migrate
```

---

## 📈 بهبودهای آینده

- [ ] Rate Limiting
- [ ] Redis Caching
- [ ] File Upload (تصاویر)
- [ ] Email Service
- [ ] SMS Service
- [ ] Payment Gateway
- [ ] WebSocket
- [ ] Unit Tests
- [ ] E2E Tests
- [ ] Docker
- [ ] CI/CD

---

## 💡 نکات مهم

### 1. Prisma Studio
برای مدیریت راحت دیتابیس:
```bash
npm run prisma:studio
```

### 2. Swagger UI
برای تست API:
```
http://localhost:4000/api/docs
```

### 3. Token در Swagger
1. ابتدا از `/auth/login` وارد شو
2. Token رو کپی کن
3. در Swagger روی "Authorize" کلیک کن
4. Token رو paste کن

### 4. Seed Data
اگر می‌خوای داده‌های اولیه رو دوباره بسازی:
```bash
npm run prisma:seed
```

---

## 🎉 تمام!

بک‌اند کاملاً آماده است! می‌تونی:

1. ✅ کاربر ثبت‌نام کنی
2. ✅ وارد بشی
3. ✅ محصول اضافه کنی
4. ✅ سفارش ثبت کنی
5. ✅ نظر بذاری
6. ✅ کاربران رو مدیریت کنی

همه چیز با Swagger مستند شده و آماده استفاده است!

---

## 📞 سوالات؟

اگر سوالی داشتی:
1. Swagger docs رو چک کن
2. BACKEND-README.md رو بخون
3. NESTJS-IMPLEMENTATION.md رو ببین

**موفق باشی! 🚀**
