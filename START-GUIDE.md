# راهنمای راه‌اندازی سریع

## مراحل راه‌اندازی

### 1️⃣ نصب Dependencies (اگر نصب نکردید)
```bash
npm install
```

### 2️⃣ راه‌اندازی دیتابیس

#### ایجاد دیتابیس و جداول
```bash
npm run prisma:migrate
```

#### پر کردن دیتابیس با داده‌های نمونه
```bash
npm run prisma:seed
```

این دستور موارد زیر را ایجاد می‌کند:
- ✅ Super Admin: `admin@maysa.com` / `Admin@123`
- ✅ Admin: `support@maysa.com` / `Admin@123`
- ✅ Customer: `user@example.com` / `User@123`
- ✅ دسته‌بندی‌ها و برندها
- ✅ محصول نمونه

### 3️⃣ اجرای بک‌اند (NestJS)
```bash
npm run nest:dev
```

بک‌اند روی پورت **4000** اجرا می‌شود:
- 🚀 API: http://localhost:4000
- 📚 Swagger Docs: http://localhost:4000/api/docs

### 4️⃣ اجرای فرانت (Next.js)
**در ترمینال جدید:**
```bash
npm run dev
```

فرانت روی پورت **3000** اجرا می‌شود:
- 🌐 Website: http://localhost:3000

## ✅ تست اتصال

1. به http://localhost:3000 بروید
2. محصولات باید از بک‌اند لود شوند
3. می‌توانید با اکانت‌های بالا لاگین کنید

## 🔧 دستورات مفید

### مشاهده دیتابیس
```bash
npm run prisma:studio
```
Prisma Studio روی http://localhost:5555 باز می‌شود

### ریست کردن دیتابیس
```bash
npx prisma migrate reset
```
این دستور دیتابیس را پاک کرده و دوباره seed می‌کند

### بیلد Production
```bash
# بیلد فرانت
npm run build

# بیلد بک‌اند
npm run nest:build

# اجرای production
npm run nest:start:prod
npm run start
```

## 📝 نکات مهم

- مطمئن شوید PostgreSQL در حال اجراست
- فایل `.env` را بررسی کنید (از `.env.example` کپی کنید)
- بک‌اند باید قبل از فرانت اجرا شود
- برای تغییرات در schema، `prisma:migrate` را اجرا کنید

## ❓ مشکلات رایج

### خطای اتصال به دیتابیس
```bash
# چک کنید PostgreSQL در حال اجراست
# DATABASE_URL در .env را بررسی کنید
```

### محصولات نمایش داده نمی‌شوند
```bash
# دیتابیس را seed کنید
npm run prisma:seed
```

### خطای CORS
```bash
# مطمئن شوید FRONTEND_URL در .env درست است
# مطمئن شوید بک‌اند در حال اجراست
```

## 📚 مستندات بیشتر

- [FRONTEND-BACKEND-CONNECTION.md](./FRONTEND-BACKEND-CONNECTION.md) - جزئیات اتصال فرانت به بک‌اند
- [راهنمای-بک-اند.md](./راهنمای-بک-اند.md) - مستندات کامل بک‌اند
- [README.md](./README.md) - مستندات اصلی پروژه
