# 🚀 دستورات اجرای پروژه

## 📋 پیش‌نیاز

1. **Node.js 18+** نصب باشه
2. **PostgreSQL 15+** نصب و اجرا باشه
3. **npm** یا **yarn** نصب باشه

---

## 🗄️ راه‌اندازی Database

### 1. نصب PostgreSQL

**Windows:**
- از [postgresql.org](https://www.postgresql.org/download/windows/) دانلود کن
- نصب کن و رمز عبور تنظیم کن

**Mac:**
```bash
brew install postgresql@15
brew services start postgresql@15
```

**Linux:**
```bash
sudo apt install postgresql postgresql-contrib
sudo systemctl start postgresql
```

### 2. ایجاد Database

```bash
# ورود به PostgreSQL
psql -U postgres

# ایجاد database
CREATE DATABASE maysa_shop;

# خروج
\q
```

---

## ⚙️ تنظیمات اولیه

### 1. نصب Dependencies

```bash
npm install
```

### 2. تنظیم Environment Variables

```bash
# کپی فایل نمونه
cp .env.example .env
```

**ویرایش `.env`:**
```env
# Database - تغییر بده
DATABASE_URL="postgresql://postgres:YOUR_PASSWORD@localhost:5432/maysa_shop"

# JWT Secret - حتماً تغییر بده
JWT_SECRET="your-super-secret-key-change-this"
JWT_EXPIRES_IN="7d"

# Server
PORT=4000
NODE_ENV="development"

# Frontend
FRONTEND_URL="http://localhost:3000"
```

### 3. راه‌اندازی Prisma

```bash
# Generate Prisma Client
npm run prisma:generate

# اجرای Migrations (ایجاد جداول)
npm run prisma:migrate

# Seed داده‌های اولیه (کاربران، محصولات نمونه)
npm run prisma:seed
```

---

## 🚀 اجرای پروژه

### حالت Development

#### Backend (NestJS)
```bash
# Terminal 1
npm run nest:dev
```
✅ Backend: http://localhost:4000
✅ Swagger: http://localhost:4000/api/docs

#### Frontend (Next.js)
```bash
# Terminal 2
npm run dev
```
✅ Frontend: http://localhost:3000

### حالت Production

#### Backend
```bash
# Build
npm run nest:build

# اجرا
npm run nest:start:prod
```

#### Frontend
```bash
# Build
npm run build

# اجرا
npm start
```

---

## 🧪 تست Backend

### 1. باز کردن Swagger
```
http://localhost:4000/api/docs
```

### 2. ورود به سیستم

**در Swagger:**
1. برو به `/auth/login`
2. کلیک روی "Try it out"
3. وارد کن:
```json
{
  "email": "admin@maysa.com",
  "password": "Admin@123"
}
```
4. Execute کن
5. Token رو کپی کن

### 3. تنظیم Token

1. بالای صفحه روی "Authorize" کلیک کن
2. Token رو paste کن (بدون Bearer)
3. Authorize کن

### 4. تست Endpoints

حالا می‌تونی همه endpoints رو تست کنی:
- ✅ `/products` - لیست محصولات
- ✅ `/categories` - دسته‌بندی‌ها
- ✅ `/orders` - سفارشات
- ✅ `/users` - کاربران (فقط Super Admin)

---

## 🔑 اطلاعات ورود پیش‌فرض

بعد از seed:

### Super Admin (دسترسی کامل)
```
ایمیل: admin@maysa.com
رمز: Admin@123
```

### Admin (مدیریت محصولات و سفارشات)
```
ایمیل: support@maysa.com
رمز: Admin@123
```

### Customer (مشتری عادی)
```
ایمیل: user@example.com
رمز: User@123
```

---

## 🛠️ دستورات مفید

### Prisma

```bash
# Prisma Studio (مدیریت دیتابیس)
npm run prisma:studio
# باز می‌شه در: http://localhost:5555

# Generate Client (بعد از تغییر schema)
npm run prisma:generate

# ایجاد Migration جدید
npx prisma migrate dev --name migration_name

# Reset Database (حذف همه داده‌ها)
npx prisma migrate reset

# Seed دوباره
npm run prisma:seed

# Format Schema
npx prisma format
```

### NestJS

```bash
# Development با watch mode
npm run nest:dev

# Build برای production
npm run nest:build

# اجرای production
npm run nest:start:prod

# لاگ‌های Prisma
DEBUG=prisma:* npm run nest:dev
```

### Next.js

```bash
# Development
npm run dev

# Build
npm run build

# Production
npm start

# Lint
npm run lint
```

---

## 📊 بررسی وضعیت

### Backend آماده است اگر:
- ✅ `npm run nest:dev` بدون خطا اجرا شد
- ✅ `http://localhost:4000` باز می‌شه
- ✅ `http://localhost:4000/api/docs` Swagger رو نشون می‌ده
- ✅ می‌تونی login کنی

### Database آماده است اگر:
- ✅ PostgreSQL اجرا شده
- ✅ `npm run prisma:migrate` موفق بود
- ✅ `npm run prisma:seed` موفق بود
- ✅ `npm run prisma:studio` باز می‌شه

### Frontend آماده است اگر:
- ✅ `npm run dev` بدون خطا اجرا شد
- ✅ `http://localhost:3000` باز می‌شه

---

## 🐛 عیب‌یابی

### خطای اتصال به Database

```bash
# بررسی PostgreSQL اجرا شده
# Windows
services.msc -> PostgreSQL

# Mac/Linux
brew services list
# یا
sudo systemctl status postgresql

# تست اتصال
psql -U postgres -d maysa_shop
```

### خطای Prisma Client

```bash
# دوباره Generate کن
npm run prisma:generate

# اگر باز هم خطا داد، node_modules رو پاک کن
rm -rf node_modules
npm install
npm run prisma:generate
```

### خطای Port در حال استفاده

```bash
# پیدا کردن process
# Windows
netstat -ano | findstr :4000

# Mac/Linux
lsof -i :4000

# Kill کردن
# Windows
taskkill /PID <PID> /F

# Mac/Linux
kill -9 <PID>
```

### خطای Migration

```bash
# Reset و دوباره Migrate
npx prisma migrate reset
npm run prisma:migrate
npm run prisma:seed
```

---

## 📝 Workflow روزانه

### شروع کار

```bash
# Terminal 1 - Backend
npm run nest:dev

# Terminal 2 - Frontend
npm run dev

# Terminal 3 - Prisma Studio (اختیاری)
npm run prisma:studio
```

### بعد از تغییر Schema

```bash
# 1. Generate Client
npm run prisma:generate

# 2. ایجاد Migration
npx prisma migrate dev --name describe_your_change

# 3. Restart Backend
# Ctrl+C و دوباره npm run nest:dev
```

### قبل از Commit

```bash
# Lint
npm run lint

# Build تست
npm run nest:build
npm run build
```

---

## 🎯 مراحل اولین اجرا (Step by Step)

### مرحله 1: نصب و تنظیم
```bash
# 1. Clone یا دانلود پروژه
cd maysa-shop

# 2. نصب dependencies
npm install

# 3. کپی .env
cp .env.example .env

# 4. ویرایش .env (DATABASE_URL و JWT_SECRET)
```

### مرحله 2: Database
```bash
# 1. اطمینان از اجرای PostgreSQL
# 2. ایجاد database
psql -U postgres -c "CREATE DATABASE maysa_shop;"

# 3. Prisma
npm run prisma:generate
npm run prisma:migrate
npm run prisma:seed
```

### مرحله 3: اجرا
```bash
# Terminal 1 - Backend
npm run nest:dev

# Terminal 2 - Frontend
npm run dev
```

### مرحله 4: تست
```bash
# 1. باز کن: http://localhost:4000/api/docs
# 2. Login کن با: admin@maysa.com / Admin@123
# 3. Token رو Authorize کن
# 4. Endpoints رو تست کن
```

---

## ✅ Checklist آماده‌سازی

- [ ] Node.js 18+ نصب شده
- [ ] PostgreSQL نصب و اجرا شده
- [ ] `npm install` اجرا شده
- [ ] `.env` ساخته و تنظیم شده
- [ ] `npm run prisma:generate` موفق بود
- [ ] `npm run prisma:migrate` موفق بود
- [ ] `npm run prisma:seed` موفق بود
- [ ] `npm run nest:dev` اجرا شد
- [ ] `http://localhost:4000/api/docs` باز می‌شه
- [ ] می‌تونم login کنم
- [ ] `npm run dev` اجرا شد
- [ ] `http://localhost:3000` باز می‌شه

---

## 🎉 آماده!

اگر همه چیز درست پیش رفت:
- ✅ Backend در http://localhost:4000
- ✅ Swagger در http://localhost:4000/api/docs
- ✅ Frontend در http://localhost:3000
- ✅ Prisma Studio در http://localhost:5555

**موفق باشی! 🚀**
