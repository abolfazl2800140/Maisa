# 🔗 اتصال فرانت به بک‌اند - انجام شد ✅

## ✨ تغییرات انجام شده

فرانت شما الان به طور کامل به بک‌اند متصل شده و داده‌ها رو از API می‌خونه.

### 1. فایل‌های جدید ساخته شده

#### 📁 `lib/api/client.ts`
یک HTTP client کامل برای ارتباط با بک‌اند:
- پشتیبانی از GET, POST, PUT, PATCH, DELETE
- مدیریت خودکار توکن JWT
- مدیریت خطاها
- TypeScript کامل

#### 📁 `lib/api/products.ts` (به‌روزرسانی شد)
قبلاً: از فایل JSON محلی می‌خوند ❌
الان: از بک‌اند API می‌خونه ✅

توابع موجود:
- `getProducts()` - همه محصولات
- `getProductBySlug(slug)` - یک محصول با slug
- `getProductsByCategory(categoryId)` - محصولات یک دسته
- `getFeaturedProducts()` - محصولات ویژه
- `searchProducts(query)` - جستجو

#### 📁 `lib/api/auth.ts` (جدید)
مدیریت احراز هویت:
- `login()` - ورود کاربر
- `register()` - ثبت‌نام
- `logout()` - خروج
- `getToken()` - دریافت توکن
- `isAuthenticated()` - چک کردن لاگین

#### 📁 `lib/api/index.ts`
Export کردن همه API ها

### 2. متغیرهای محیطی

به فایل `.env` اضافه شد:
```env
NEXT_PUBLIC_API_URL="http://localhost:4000"
```

## 🚀 نحوه اجرا

### مرحله 1: راه‌اندازی دیتابیس
```bash
npm run prisma:migrate
npm run prisma:seed
```

### مرحله 2: اجرای بک‌اند
```bash
npm run nest:dev
```
✅ بک‌اند روی http://localhost:4000 اجرا می‌شه

### مرحله 3: اجرای فرانت (ترمینال جدید)
```bash
npm run dev
```
✅ فرانت روی http://localhost:3000 اجرا می‌شه

## 🎯 تست کنید

1. بک‌اند رو اجرا کنید
2. فرانت رو اجرا کنید  
3. به http://localhost:3000 برید
4. محصولات باید از بک‌اند لود بشن! 🎉

## 💡 نکات مهم

### توکن JWT
- توکن در `localStorage` ذخیره می‌شه
- به صورت خودکار در header های request ها ارسال می‌شه
- برای API های محافظت شده لازمه

### CORS
- در بک‌اند فعال شده ✅
- فرانت می‌تونه به API دسترسی داشته باشه

### مدیریت خطا
- خطاها به صورت خودکار مدیریت می‌شن
- در console نمایش داده می‌شن
- در صورت خطا، آرایه خالی برمی‌گردونه (برای products)

## 🔍 مثال استفاده

### در کامپوننت‌ها
```typescript
import { getProducts, getFeaturedProducts } from '@/lib/api/products';

// استفاده در کامپوننت
const products = await getProducts();
const featured = await getFeaturedProducts();
```

### لاگین کاربر
```typescript
import { login } from '@/lib/api/auth';

const handleLogin = async () => {
  try {
    const response = await login({
      email: 'user@example.com',
      password: 'User@123'
    });
    console.log('لاگین موفق:', response.user);
  } catch (error) {
    console.error('خطا در لاگین:', error);
  }
};
```

## 🛠️ دستورات مفید

### مشاهده دیتابیس
```bash
npm run prisma:studio
```

### ریست دیتابیس
```bash
npx prisma migrate reset
```

### مشاهده API Docs
بک‌اند رو اجرا کنید و به این آدرس برید:
http://localhost:4000/api/docs

## ❓ مشکلات احتمالی

### محصولات نمایش داده نمی‌شن
1. مطمئن شید بک‌اند در حال اجراست
2. دیتابیس رو seed کنید: `npm run prisma:seed`
3. Console browser رو چک کنید

### خطای CORS
1. مطمئن شید `FRONTEND_URL` در `.env` درسته
2. بک‌اند رو restart کنید

### خطای اتصال به دیتابیس
1. PostgreSQL رو چک کنید
2. `DATABASE_URL` در `.env` رو بررسی کنید

## 📚 فایل‌های مرتبط

- [START-GUIDE.md](./START-GUIDE.md) - راهنمای راه‌اندازی سریع
- [FRONTEND-BACKEND-CONNECTION.md](./FRONTEND-BACKEND-CONNECTION.md) - جزئیات تکنیکال
- [راهنمای-بک-اند.md](./راهنمای-بک-اند.md) - مستندات بک‌اند

---

## ✅ خلاصه

فرانت شما الان:
- ✅ به بک‌اند متصل شده
- ✅ محصولات رو از API می‌خونه
- ✅ احراز هویت داره
- ✅ مدیریت خطا داره
- ✅ TypeScript کامل داره

**همه چی آماده است! فقط بک‌اند و فرانت رو اجرا کنید** 🚀
